[project]
name = "beamer-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "selectolax>=0.3.21",
    "pandas>=2.2.0",
    "duckdb>=1.0.0",
    "clickhouse-connect>=0.8.0",
]

[tool.setuptools]
packages = ["src"]
